# 📦 백엔드 패키지 구조 설계서

> **프로젝트명:** ForPets (포펫츠)
> **팀명:** 집사조
> **버전:** v1.1
> **패키지 루트:** `com.example.forpets`
> **최종 수정일:** 2026-05-13
> **연결 문서:** 04_기능_명세서, 10_ADR

---

## 핵심 설계 결정 요약

| 항목 | 결정 | 이유 |
|------|------|------|
| 구조 방식 | 도메인형 최상위 + 계층형 내부 | 도메인 간 결합도 최소화, 팀원별 도메인 독립 개발 |
| 분산락 공통 모듈 | `global/lock` | 매칭 도메인에서 Redisson 락 로직 공유 |
| Kafka 이벤트 | `global/kafka` Producer/Consumer | 매칭 확정 이벤트를 여러 도메인이 공유 |
| SSE 공통 관리 | `global/sse` | 알림 발송 로직을 단일 모듈로 통합 |
| QueryDSL Custom 분리 | `*RepositoryCustom` | 동적 검색 쿼리를 JPA Repository와 분리 (ADR-007) |
| 에러 단일 정의 | `global/exception/ErrorCode` | 모든 에러를 enum 한 곳에서 관리 |

---

## 1. 전체 패키지 트리

```
src/main/java/com/example/forpets/
│
├── global/                         # 앱 전체 공통 관심사
│   ├── config/                     # 인프라 · 프레임워크 설정
│   ├── security/                   # JWT 인증 필터 체인
│   ├── lock/                       # Redisson 분산락 공통 모듈
│   ├── kafka/                      # Kafka Producer/Consumer 공통
│   ├── sse/                        # SSE 알림 공통 관리
│   ├── exception/                  # 공통 예외 정의 · 핸들러
│   ├── common/                     # 공통 응답 DTO · BaseEntity
│   └── util/                       # 범용 유틸리티
│
└── domain/                         # 비즈니스 도메인
    ├── auth/                       # 인증 (회원가입 · 로그인 · JWT)
    ├── user/                       # 회원 프로필 · 반려동물 · 시터
    ├── posting/                    # 케어 공고 CRUD · 검색
    ├── proposal/                   # 시터 제안 입찰 · 매칭 확정
    ├── matching/                   # 매칭 상태 관리 · 케어 일지
    ├── review/                     # 후기 · 평점
    └── notification/               # 알림 이력 관리
```

---

## 2. `global` 패키지 상세

```
global/
│
├── config/
│   ├── SecurityConfig.java         # Spring Security 필터 체인, 공개 URL 화이트리스트
│   │                               # 공개: GET /api/postings, GET /api/sitters, POST /api/auth/signup, POST /api/auth/login
│   │                               # 인증: POST /api/auth/logout, 나머지 모든 API
│   ├── RedisConfig.java            # RedissonClient + RedisTemplate 설정
│   │                               # Redisson: 분산락용 (lock:proposal:*)
│   │                               # RedisTemplate: 캐싱·SSE 세션·ZSET 평점 랭킹·JWT 블랙리스트용
│   ├── KafkaConfig.java            # KafkaProducer·Consumer 설정
│   │                               # topic: matching-confirmed
│   ├── WebSocketConfig.java        # STOMP 브로커 · 엔드포인트 (/ws-stomp) 등록
│   ├── QuerydslConfig.java         # JPAQueryFactory 빈 등록
│   └── CacheConfig.java            # Redis 캐시 매니저 설정 (공고 TTL 5분, 시터 TTL 1시간)
│
├── security/
│   ├── JwtUtil.java                # 토큰 생성 · 검증 · Claim 추출
│   │                               # payload: { userId, role(GUARDIAN/SITTER/RESCUE), iat, exp }
│   │                               # AccessToken: HS256, 만료 1시간 (ADR-001)
│   │                               # RefreshToken: 7일, Redis 저장
│   ├── JwtAuthFilter.java          # OncePerRequestFilter — Authorization 헤더 파싱
│   │                               # Redis 블랙리스트 조회 → 등록된 토큰 401 반환
│   ├── JwtAuthEntryPoint.java      # 401 Unauthorized 응답 처리
│   ├── JwtAccessDeniedHandler.java # 403 Forbidden 응답 처리
│   └── CustomUserDetails.java      # UserDetails 구현체 (userId, email, role 보관)
│
├── lock/
│   ├── RedisLock.java              # @RedisLock 커스텀 어노테이션 (AOP, 도전)
│   │                               # key: SpEL 표현식, waitTime, leaseTime
│   ├── RedisLockAspect.java        # @Around — Redisson tryLock + unlock (AOP, 도전)
│   └── LockKeyConstants.java       # 락 키 상수 모음
│                                   # PROPOSAL_LOCK_PREFIX = "lock:proposal:"
│
├── kafka/
│   ├── producer/
│   │   └── MatchingEventProducer.java   # matching-confirmed 토픽 발행
│   ├── consumer/
│   │   ├── PaymentConsumer.java         # matching-confirmed → 결제 처리
│   │   ├── NotificationConsumer.java    # matching-confirmed → SSE 알림 발송
│   │   └── ScheduleConsumer.java        # matching-confirmed → 케어 일정 생성
│   └── event/
│       └── MatchingConfirmedEvent.java  # Kafka 이벤트 페이로드 DTO
│
├── sse/
│   ├── SseEmitterManager.java      # SseEmitter 등록 · 해제 · 이벤트 전송 관리
│   ├── SseEventType.java           # CARE_LOG, PROPOSAL_ARRIVED, MATCHING_CONFIRMED
│   └── SseEventPayload.java        # SSE 이벤트 페이로드 DTO
│
├── exception/
│   ├── ErrorCode.java              # ⭐ 에러코드 enum 단일 정의
│   ├── GlobalExceptionHandler.java # @RestControllerAdvice
│   ├── BusinessException.java      # 비즈니스 예외 최상위 (ErrorCode 보유)
│   ├── NotFoundException.java      # 404
│   ├── ConflictException.java      # 409
│   ├── ForbiddenException.java     # 403
│   ├── InvalidRequestException.java# 400
│   └── TooManyRequestsException.java# 429 (Redisson 락 획득 실패 전용)
│
├── common/
│   ├── ApiResponse.java            # 공통 응답 래퍼 { success, data, message }
│   │                               # 성공·실패 모두 ApiResponse<T> 사용
│   └── BaseTimeEntity.java         # @MappedSuperclass — created_at, updated_at 공통 상속
│
└── util/
    └── SecurityUtil.java           # SecurityContextHolder에서 userId, role 추출 헬퍼
```

---

## 3. `domain` 패키지 상세

### 3-1. `auth` — 인증

```
domain/auth/
├── controller/
│   └── AuthController.java         # POST /api/auth/signup
│                                   # POST /api/auth/login
│                                   # POST /api/auth/logout   (JWT 블랙리스트 등록)
│                                   # POST /api/auth/refresh  (AccessToken 재발급)
├── dto/
│   ├── request/
│   │   ├── SignupRequestDto.java    # { email, password, nickname, role } + @Valid
│   │   └── LoginRequestDto.java    # { email, password }
│   └── response/
│       ├── SignupResponseDto.java   # { userId, email, role, createdAt }
│       └── LoginResponseDto.java   # { accessToken, refreshToken, expiresIn, tokenType }
└── service/
    └── AuthService.java            # 회원가입(BCrypt 암호화, 역할별 프로필 초기화)
                                    # 로그인(JWT 발급) — payload: userId, role
                                    # 로그아웃: AccessToken → Redis 블랙리스트 등록
                                    # 토큰 갱신: RefreshToken 검증 → AccessToken 재발급
```

> **JWT 정책 (ADR-001):** AccessToken 1시간, RefreshToken 7일 Redis 저장.
> 역할(role)은 JWT payload에 포함 → `@PreAuthorize("hasRole('GUARDIAN')")` 접근 제어.
> RESCUE 역할은 구조단체 전용 API(@PreAuthorize("hasRole('RESCUE')"))에서 사용.

---

### 3-2. `user` — 회원 · 반려동물 · 시터 프로필

```
domain/user/
├── controller/
│   ├── UserController.java         # GET  /api/users/me
│   │                               # PATCH /api/users/me
│   │                               # GET  /api/users/me/matchings (매칭 내역)
│   ├── PetController.java          # POST /api/pets            (반려동물 등록)
│   │                               # GET  /api/pets            (내 반려동물 목록)
│   │                               # PUT  /api/pets/{petId}    (수정)
│   │                               # DELETE /api/pets/{petId}  (삭제)
│   └── SitterProfileController.java# GET  /api/sitters/{userId}         (프로필 조회)
│                                   # PUT  /api/users/me/sitter           (수정)
│
├── dto/
│   ├── request/
│   │   ├── UpdateUserRequestDto.java
│   │   ├── PetCreateRequestDto.java
│   │   └── SitterProfileUpdateDto.java
│   └── response/
│       ├── UserProfileResponseDto.java
│       ├── PetResponseDto.java
│       └── SitterProfileResponseDto.java
│
├── entity/
│   ├── User.java                   # USER 테이블
│   ├── Pet.java                    # PET 테이블
│   └── SitterProfile.java          # SITTER_PROFILE 테이블
│
├── enums/
│   ├── UserRole.java               # GUARDIAN | SITTER | RESCUE
│   ├── PetSpecies.java             # DOG | CAT | ETC
│   └── PetSize.java                # SMALL | MEDIUM | LARGE
│
├── repository/
│   ├── UserRepository.java
│   ├── PetRepository.java
│   └── SitterProfileRepository.java
│
└── service/
    ├── UserService.java
    ├── PetService.java
    └── SitterProfileService.java
```

---

### 3-3. `posting` — 케어 공고 CRUD · 검색

```
domain/posting/
├── controller/
│   └── PostingController.java      # POST /api/postings             (공고 등록)
│                                   # GET  /api/postings             (목록 검색 💾)
│                                   # GET  /api/postings/{id}        (상세)
│                                   # PATCH /api/postings/{id}       (수정)
│                                   # PATCH /api/postings/{id}/close (취소)
│
├── dto/
│   ├── request/
│   │   ├── PostingCreateRequestDto.java
│   │   └── PostingSearchCondition.java
│   └── response/
│       └── PostingResponseDto.java
│
├── entity/
│   └── CarePosting.java            # status: OPEN | MATCHED | COMPLETED | CLOSED
│
├── enums/
│   └── PostingStatus.java
│
├── repository/
│   ├── PostingRepository.java
│   └── PostingRepositoryCustom.java # QueryDSL 동적 검색
│
└── service/
    └── PostingService.java
```

---

### 3-4. `proposal` — 시터 제안 입찰

```
domain/proposal/
├── controller/
│   └── ProposalController.java     # POST /api/postings/{id}/proposals  (제안 입찰 ⚠️)
│                                   # GET  /api/postings/{id}/proposals  (제안 목록)
│                                   # POST /api/proposals/{id}/accept    (수락)
│                                   # POST /api/proposals/{id}/reject    (거절)
│
├── dto/
│   ├── request/
│   │   └── ProposalCreateRequestDto.java
│   └── response/
│       └── ProposalResponseDto.java
│
├── entity/
│   └── SitterProposal.java         # status: PENDING | ACCEPTED | REJECTED
│
├── enums/
│   └── ProposalStatus.java
│
├── repository/
│   └── ProposalRepository.java
│
└── service/
    └── ProposalFacade.java         # ⭐ 제안 입찰 (Redisson 분산락, ADR-002)
                                    # 락 키: lock:proposal:{postingId}
                                    # waitTime=3s (Fail Fast), leaseTime=10s
                                    # 락 획득 → ProposalService.submitProposalInTx() 호출
```

---

### 3-5. `matching` — 매칭 상태 관리 · 케어 일지

```
domain/matching/
├── controller/
│   ├── MatchingController.java     # PATCH /api/matchings/{id}/complete  (케어 완료)
│   └── CareLogController.java      # POST /api/matchings/{id}/logs       (일지 등록)
│                                   # GET  /api/matchings/{id}/logs       (일지 목록)
│
├── dto/
│   ├── request/
│   │   └── CareLogCreateRequestDto.java
│   └── response/
│       ├── MatchingResponseDto.java
│       └── CareLogResponseDto.java
│
├── entity/
│   ├── Matching.java               # MATCHING 테이블
│   │                               # posting_id UNIQUE — 중복 매칭 DB 레벨 차단 (ADR-005)
│   │                               # status: OPEN | MATCHED | COMPLETED | CLOSED
│   └── CareLog.java                # CARE_LOG 테이블
│
├── enums/
│   ├── MatchingStatus.java         # OPEN | MATCHED | COMPLETED | CLOSED | REVIEWED
│   └── PaymentStatus.java          # PENDING | PAID | REFUNDED
│
├── repository/
│   ├── MatchingRepository.java
│   └── CareLogRepository.java
│
└── service/
    ├── MatchingService.java        # 제안 수락 → Matching 생성 (posting_id UNIQUE 보호)
    │                               # → Kafka matching-confirmed 이벤트 발행 (ADR-003)
    │                               # → CarePosting.status = MATCHED
    │                               # → 나머지 Proposal.status = REJECTED
    └── CareLogService.java         # 케어 일지 저장 → SSE 발송 (CARE_LOG 이벤트)
```

---

### 3-6. `review` — 후기 · 평점

```
domain/review/
├── controller/
│   └── ReviewController.java       # POST /api/reviews                   (후기 작성)
│                                   # GET  /api/sitters/{userId}/reviews  (시터 후기 목록 💾)
│
├── dto/
│   ├── request/
│   │   └── ReviewCreateRequestDto.java  # { matchingId, rating, content }
│   └── response/
│       └── ReviewResponseDto.java
│
├── entity/
│   └── Review.java                 # REVIEW 테이블
│                                   # matching_id UNIQUE — 1매칭 1후기 강제
│                                   # 작성 조건: Matching.status == COMPLETED
│                                   # 보호자·시터 양방향 후기 작성 가능
│
├── repository/
│   └── ReviewRepository.java
│
└── service/
    └── ReviewService.java          # 후기 저장 + Redis ZSET 평점 갱신 (ZINCRBY)
```

---

### 3-7. `notification` — 알림 이력

```
domain/notification/
├── controller/
│   ├── NotificationController.java # GET /api/notifications         (알림 목록)
│   │                               # GET /api/notifications/stream  (SSE 구독 📡)
│   └── SseController.java          # SSE 연결 관리
│
├── entity/
│   └── Notification.java           # NOTIFICATION 테이블
│                                   # type: PROPOSAL_ARRIVED | MATCHING_CONFIRMED | CARE_LOG
│                                   # is_read: 미읽음 여부
│
└── service/
    └── NotificationService.java    # 알림 저장 + SSE 발송
                                    # SSE 연결 끊김: 알림 DB 저장 → 재연결 시 미읽음 제공
```

---

## 4. 전체 파일 수 요약

| 패키지 | Controller | Service | Repository | Entity | DTO | 기타 | 합계 |
|--------|-----------|---------|------------|--------|-----|------|------|
| global | — | — | — | 1(Base) | 1 | ~18개 | ~20개 |
| auth | 1 | 1 | — | — | 4 | — | 6개 |
| user | 3 | 3 | 3 | 3 | 6 | 3(enums) | 21개 |
| posting | 1 | 1 | 2 | 1 | 3 | 1(enum) | 9개 |
| proposal | 1 | 1 | 1 | 1 | 2 | 1(enum) | 7개 |
| matching | 2 | 2 | 2 | 2 | 4 | 2(enums) | 14개 |
| review | 1 | 1 | 1 | 1 | 2 | — | 6개 |
| notification | 2 | 1 | 1 | 1 | 2 | 1(enum) | 8개 |
| **합계** | **11** | **10** | **10** | **9** | **23** | **26** | **약 91개** |

---

## 변경 이력

| 버전 | 변경 내용 | 일자 |
|------|----------|------|
| v1.0 | 최초 작성 | 2026-05-12 |
| v1.1 | 팀원 이름 전체 제거, AI 기능 격리 row 삭제, rental·tempcare·chat 도메인 삭제, 로그아웃·블랙리스트·RefreshToken 반영, 매칭 상태명 v1.1 기준 동기화, 양방향 후기 명시, ApiResponse 공통 응답 반영 | 2026-05-13 |